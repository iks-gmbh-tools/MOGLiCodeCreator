#This file is used to define MetaInfoValidators.
#Syntax: Validator <validator type> <MetaInfo Name> <MetaInfo HierarchyLevel> <name of valid model>
#For Mandatory validators, the MetaInfo must occur at least for each elements in the validator's hierarchy level.
#For Optional validators, the validation result is always true. They are used for statistical purpose 
#A validator is applied for any model if no valid model name is defined. Otherwise a validator is applied only to the defined valid model.
#(see output/StandardModelProvider/ModelStatistics.txt).

Validator Mandatory JavaType Attribute DemoModel
Validator Optional extends Class DemoModel
Validator Optional implements Class DemoModel

Validator Mandatory ExampleData Attribute DemoModel
Validator Optional Mandatory Attribute DemoModel
Validator Optional MinLength Attribute DemoModel
Validator Optional MaxLength Attribute DemoModel